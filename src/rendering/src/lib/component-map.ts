/**
 * Component Map for Content SDK
 * This maps Sitecore component names to React components
 *
 * Note: This is a TEMPLATE component map. The actual PLAY! Summit components
 * require migration from JSS to Content SDK APIs before they can be used.
 *
 * Each component currently imports from '@sitecore-jss/sitecore-jss-nextjs'
 * and needs to be updated to use '@sitecore-content-sdk/nextjs' APIs.
 *
 * See migration documentation for component update process.
 */

/**
 * Component map for Content SDK
 * Maps Sitecore component names to React components
 *
 * TODO: Import and register all PLAY! Summit components after migrating them to Content SDK
 */
export const componentMap: Record<string, React.ComponentType<unknown>> = {
  // Components will be registered here after migration
  // Example:
  // ContentBlock: ContentBlockComponent,
  // Promo: PromoComponent,
  // etc.
};

export default componentMap;


